const { input, select } = require('@inquirer/prompts');


(async function() {
    // Indiquez un nombre
    // Indiquez un nombre
    // Indiquez un nombre


    // Afficher les nombres du plus petit au plus grand 


    let taille = await input({ message: 'Indiquez votre taille en cm' });
 
})()